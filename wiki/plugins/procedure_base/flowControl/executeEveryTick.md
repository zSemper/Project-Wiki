# Execute Every Tick

Executes the code inside at an interval of every x ticks.

![Execute Every Tick](/resources/plugins/procedure_base/flowControl/executeEveryTick.png)

## Parameter

Every `ticks` ticks do

|Definition|Description|
|-|-|
|ticks|The interval between each operator to run|